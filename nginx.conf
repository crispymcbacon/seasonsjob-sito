server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location = /site {
    return 301 https://lavoro.seasonsjob.it$request_uri;
  }
  location ^~ /site/ {
    return 301 https://lavoro.seasonsjob.it$request_uri;
  }

  location = /app {
    return 301 https://lavoro.seasonsjob.it$request_uri;
  }
  location ^~ /app/ {
    return 301 https://lavoro.seasonsjob.it$request_uri;
  }

  location = /api {
    return 301 https://lavoro.seasonsjob.it$request_uri;
  }
  location ^~ /api/ {
    return 301 https://lavoro.seasonsjob.it$request_uri;
  }

  location /_astro/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
  }

  location / {
    try_files $uri $uri/ $uri.html =404;
  }

  error_page 404 /404.html;
}
