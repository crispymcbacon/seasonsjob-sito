---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PagePlaceholder from '@/components/ui/PagePlaceholder.astro';

const pageTitle = 'Contatti';
---

<BaseLayout
  title={pageTitle}
  description="Pagina segnaposto per l'area Contatti."
>
  <PagePlaceholder
    title={pageTitle}
    description="Pagina segnaposto per l'area Contatti. I contenuti reali verranno scritti in una fase successiva."
  />
</BaseLayout>
